<form method="POST" action="/api/responses/{{survey.id}}/{{step.id}}">
  <input type="hidden" name="responseId" value="{{responseId}}">

  <table class="grid-table">
    <thead>
      <tr>
        <th></th>
        {{#step.reponses}}
          <th>{{label}}</th>
        {{/step.reponses}}
      </tr>
    </thead>

    <tbody>
      {{#step.questions}}  <!-- rows -->
      <tr>
        <td class="row-label">{{label}}</td>

        {{#columns}}  <!-- columns générés par prepareGridB -->
        <td class="grid-cell">
          {{#isRadio}}
            <input
              type="radio"
              name="{{name}}"
              value="{{value}}"
              {{#checked}}checked{{/checked}}
            >
          {{/isRadio}}

          {{#isCheckbox}}
            <input
              type="checkbox"
              name="{{name}}"
              value="{{value}}"
              {{#checked}}checked{{/checked}}
            >
          {{/isCheckbox}}
        </td>
        {{/columns}}

      </tr>
      {{/step.questions}}
    </tbody>
  </table>

  <button type="submit">Suivant</button>
</form>
